# ML Integration

The Central Web prediction screen consumes typed prediction records and exposes reviewer actions. ML services may write prediction results through an API contract, but only an authorized reviewer can transition `AI_DRAFT` to `APPROVED`.
